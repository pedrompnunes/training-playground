pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
		    steps {
			
			sh repeat
			sh 'sleep 10s'
			sh repeat
		    }
        }
    }
}

def repeat(){
	sh 'date >> text.txt'
}
