pipeline {
    agent { 
		docker { 
			image 'alpine:latest'
			args '"NOTIFICATION_EMAIL=email@email.com"'
		}
	}
    stages {
        stage('build') {
            steps {
                echo "$NOTIFICATION_EMAIL"
            }
        }
    }
}
